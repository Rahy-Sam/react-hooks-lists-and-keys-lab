import React from "react";

function NavBar() {
  const links = ["home", "about", "projects"];
  const linkItems= links.map((link,i) =>
  <a>{home}</a>,<a>{about},<a>{projects}</a></a>
 );

  

  return <nav>{ <a>{home}</a>,<a>{about},<a>{projects}</a></a>}</nav>;
}

export default NavBar;